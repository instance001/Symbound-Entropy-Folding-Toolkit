# Example Task Set Description

This example uses:
- dim = 3 capacity dimensions
- task_count = 50
- max_demand = 1.0

Tasks are randomly generated using `task_generator.random_task_set`.
The capacity vector is [1.0, 1.0, 1.0] by default.
